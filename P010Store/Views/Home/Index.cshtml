@model HomePageViewModel


@{
    ViewData["Title"] = "Ana Sayfa";
}

<!-- Carousel -->
<partial name="_Carousel" model="Model.Carousels"/>

@* @section Scripts{
        <script>
            $(".carousel-item:first").addClass("active") // slider ın ilk slide ı active olsun diye sen razor syntax ile yaptın
            $(".carousel-indicators:first").addClass("active") // bu slider aşağıda slide belirteç buton sayılarını aktifleştirme
        </script>
    }*@




 <!-- Header-->
    <header class="bg-dark py-5 mt-5">
        <div class="container px-4 px-lg-5 my-5">
            <div class="text-center text-white">
                <h1 class="display-4 fw-bolder">Ürünlerimiz</h1>
                <p class="lead fw-normal text-white-50 mb-0">Sizin için seçtiklerimiz</p>
            </div>
        </div>
    </header>

<partial name="_Products" model="Model.Products" />
@*<!-- Section-->
<section class="py-5">
    <div class="container px-4 px-lg-5 mt-5">
        <div class="row gx-4 gx-lg-5 row-cols-2 row-cols-md-3 row-cols-xl-4 justify-content-center">
           
           @foreach (var item in Model.Products)
           {
                <div class="col mb-5">
                <div class="card h-100">
                    <!-- Product image-->
                    <img class="card-img-top" src="/Img/Products/@item.Image" alt="@item.Name" title="@item.Name"/>
                    <!-- Product details-->
                    <div class="card-body p-4">
                        <div class="text-center">
                            <!-- Product name-->
                            <h5 class="fw-bolder"> @item.Name</h5>
                            <!-- Product price-->
                            @item.Price ₺
                        </div>
                    </div>
                    <!-- Product actions-->
                    <div class="card-footer p-4 pt-0 border-top-0 bg-transparent">
                        <div class="text-center"><a class="btn btn-outline-dark mt-auto" href="/Products/Detail/@item.Id">İncele</a></div>
                    </div>
                </div>
            </div>
           }


        </div>
    </div>
</section>*@


<!-- Header-->
<header class="bg-dark py-5 my-5">
    <div class="container px-4 px-lg-5 my-5">
        <div class="text-center text-white">
            <h1 class="display-4 fw-bolder">Markalar</h1>
            <p class="lead fw-normal text-white-50 mb-0">En iyi markalar burada</p>
        </div>
    </div>
</header>
<div class="row">
    @foreach (var item in Model.Brands)
    {
        <div class="col">
            <a asp-controller="Brands" asp-action="Index" asp-route-id="@item.Id">
                 <img src="/Img/@item.Logo" alt="@item.Name" title="@item.Name" height="60"/>
            </a>
        </div>
    }
</div>


